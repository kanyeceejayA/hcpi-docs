Simple site for HCPI insytallation Process.

on the required files, mention that those are test files from uganda's case. to start with files from elsewhere, need to go through cloning the repository and code from their server, with hyperlink to that page

That new page is for extraction from a linx server

in terminal/bash
ssh into server, e.g. ssh uboscp@uboscpi.ubos.org -p {port number}
you will be prompted to enter password
will need server credentials,

ps auxf | grep -E 'odoo-bin|hcpi' to find the running service.